# Example env file for Supabase Functions (DO NOT COMMIT real keys)
# Copy this to `.env.functions` or another private file and populate the values.

# Your Supabase project URL (e.g. https://xyzcompany.supabase.co)
SUPABASE_URL=https://your-project-id.supabase.co

# Service Role Key (RESTRICTED) — use only for server-side functions.
# Keep this secret and do NOT commit to version control.
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Optional: project ref (if you use CLI flags)
#SUPABASE_PROJECT_REF=your-project-ref
